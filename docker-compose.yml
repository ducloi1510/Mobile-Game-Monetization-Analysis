services:
    postgre_sql:
        image: postgres:16
        environment:
            POSTGRES_PASSWORD: abcde
            POSTGRES_DB: game_db
        ports:
            - "5432:5432"
        volumes:
            - pg_data:/var/lib/postgresql/data
            - ./data:/data

volumes:
  pg_data: